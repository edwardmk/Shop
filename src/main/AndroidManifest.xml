<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.c15395091.shop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Reviews"></activity>
        <activity android:name=".Adapter.CartListAdapter" />
        <activity
            android:name=".Template.AbstractCardValidation"
            android:label="@string/title_activity_abstract_card_validation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Cart" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddItem" />
        <activity android:name=".AdminHomePage" />
        <activity android:name=".AdminRegister" />
        <activity android:name=".DisplayItems" />
        <activity android:name=".HomePage" />
        <activity android:name=".Register" />
        <activity android:name=".Payment" />
    </application>

</manifest>